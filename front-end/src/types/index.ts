export interface AccountTypes {
    name?: string,
    email?: string,
    password?: string,
    password_confirmation?: string,
}
export interface AddPosteTypes {
    Description: string,
    Picture: null
}